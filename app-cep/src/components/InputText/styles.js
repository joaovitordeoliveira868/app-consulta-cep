import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderRadius: 5,
    color: "#4CAF50",
    width: "100%",
    borderWidth: 2,
    borderColor: '#4CAF50',
    paddingHorizontal: 15,
    paddingVertical: 10,
    marginTop: 10,
    fontSize: 16,
  }
});
